/* =====================================================================
   ARQUIVO DESATIVADO — NÃO USE.
   =====================================================================
   Este era uma cópia do código de SERVIDOR (/api/content.js) que ficava
   dentro de /assets/, ou seja, servida publicamente em
   https://cetecritic.xyz/assets/content.js. Isso expunha toda a lógica do
   painel admin (ações, moderação, verificação de admin) a qualquer visitante.

   Observação importante: esta cópia estava MAIS NOVA que /api/content.js —
   tinha recursos de anonimato por período (anon_ate, anon_modo, alternarAnon)
   que NUNCA foram para produção, porque a Vercel só executa arquivos em /api/.
   Se esses recursos forem desejados, portar para /api/content.js e /api/db.js.

   Conteúdo removido por segurança em 2026-07-27.
   Recomendado apagar do repositório: git rm assets/content.js
   ===================================================================== */
