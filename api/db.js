/* =====================================================================
   ARQUIVO DESATIVADO — NÃO USE.
   =====================================================================
   Este era uma cópia do código de SERVIDOR (/api/db.js) que ficava dentro
   de /assets/, ou seja, servida publicamente em https://cetecritic.xyz/assets/db.js.
   Isso expunha toda a lógica do backend (tabelas, verificação de token,
   comparações de segredo, hashing de senha, regras de admin) a qualquer
   visitante. Não continha segredos literais (usa process.env), mas a
   exposição da lógica é um risco desnecessário.

   O backend REAL roda apenas em /api/ (funções serverless da Vercel). O
   frontend fala com ele via API_URL = '/api/db'. Esta cópia estava morta.

   Conteúdo removido por segurança em 2026-07-27.
   Recomendado apagar do repositório: git rm assets/db.js
   ===================================================================== */
